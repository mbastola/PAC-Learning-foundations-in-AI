# Formal Area

- `coq/` contains a minimal Coq sketch for finite agreement on examples.
- `prolog/` contains a tiny logic-programming sketch for entailment and
  resolution-style clauses.

The main repository is still LaTeX plus Python. The formal files are here to
show where mechanically checked fragments belong, not to rewrite the whole
course in a verifier.

